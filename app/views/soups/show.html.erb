<h2><%= @soup.name %></h2>


<% if @soup.featured? %>
  <p>This delicious soup is featured this month.</p>
<% else %>
  <p>This soup is not in sesson, so it kinda sucks right now.</p>
<% end %>

<% if params[:admin] == "true" %>
<p><%= link_to "Toggle Featured", toggle_featured_path(@soup) %></p>
<% end %>
